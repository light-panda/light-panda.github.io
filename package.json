{
  "name": "light-panda.github.io",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.6",
    "is-mobile": "^2.2.0",
    "rc-queue-anim": "^1.8.2",
    "react": "^16.10.2",
    "react-beautiful-dnd": "^12.1.1",
    "react-contenteditable": "^3.3.3",
    "react-dom": "^16.10.2",
    "react-scripts": "3.2.0",
    "react-transition-group": "^4.3.0",
    "use-hooks": "^2.0.0-rc.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "yarn build && git add build && git commit -m 'built' && git push && gh-pages -d build && git checkout master && git pull origin gh-pages && git push origin master && git checkout react-app"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^2.1.1"
  }
}
